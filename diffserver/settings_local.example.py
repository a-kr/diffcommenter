# coding: utf-8
"""

    This is the local configuration of diffcommenter.
    Copy it to `settings_local.py` and change whatever settings you need.

"""
from settings import *

# URL of diffcommenter server (displayed on the index page,
# in the config example)
ROOT_URL = 'http://localhost:8200'
